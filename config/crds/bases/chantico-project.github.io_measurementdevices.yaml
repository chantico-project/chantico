---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: measurementdevices.chantico-project.github.io
spec:
  group: chantico-project.github.io
  names:
    kind: MeasurementDevice
    listKind: MeasurementDeviceList
    plural: measurementdevices
    singular: measurementdevice
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MeasurementDevice is the Schema for the measurementdevices API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MeasurementDeviceSpec defines the desired state of MeasurementDevice
            properties:
              auth:
                description: 'Courtesy of the snmp_exporter repository: https://github.com/prometheus/snmp_exporter/blob/main/config/config.go'
                properties:
                  auth_protocol:
                    type: string
                  community:
                    type: string
                  context_name:
                    type: string
                  password:
                    type: string
                  priv_password:
                    type: string
                  priv_protocol:
                    type: string
                  security_level:
                    type: string
                  username:
                    type: string
                  version:
                    type: integer
                type: object
              walks:
                description: Walks is the list of SNMP OID walk paths to perform on
                  the device.
                items:
                  type: string
                type: array
            required:
            - auth
            - walks
            type: object
          status:
            description: MeasurementDeviceStatus defines the observed state of MeasurementDevice
            properties:
              errorMessage:
                type: string
              jobName:
                type: string
              state:
                description: |-
                  INSERT ADDITIONAL STATUS FIELD - define observed state of cluster
                  Important: Run "make" to regenerate code after modifying this file
                type: string
              updateGeneration:
                format: int64
                type: integer
              updateTime:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
