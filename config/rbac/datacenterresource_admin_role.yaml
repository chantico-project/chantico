# This rule is not used by the project chantico itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants full permissions ('*') over chantico-project.github.io.
# This role is intended for users authorized to modify roles and bindings within the cluster,
# enabling them to delegate specific permissions to other users or groups as needed.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: chantico
    app.kubernetes.io/managed-by: kustomize
  name: datacenterresource-admin-role
rules:
- apiGroups:
  - chantico-project.github.io
  resources:
  - datacenterresources
  verbs:
  - '*'
- apiGroups:
  - chantico-project.github.io
  resources:
  - datacenterresources/status
  verbs:
  - get
