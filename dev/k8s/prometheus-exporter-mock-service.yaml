apiVersion: v1
kind: Service
metadata:
  name: chantico-prometheus-exporter-mock
spec:
  selector:
    app: chantico-prometheus-exporter-mock
  ports:
  - protocol: TCP
    port: 9090
    targetPort: 9090
    nodePort: 30909  # NodePort (must be between 30000–32767)
  type: NodePort
